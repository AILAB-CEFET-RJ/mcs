global:
  seed: 42
  n_jobs_xgb: 8
  early_stopping_rounds: 5
  runs_dir: "runs"
  run_label: "stage2_smoke"
  output_tag: "OPTIMIZED_SMOKE"
  final_seeds: [987]
  trials_per_round: [2, 2]
  directions:
    - minimize
    - minimize
    - minimize
    - minimize
    - minimize
    - minimize
    - minimize
    - minimize

experiments:
  - dataset: data/datasets/RN_WEEKLY/dataset.pickle
    models: [poisson]
    name: RN_WEEKLY
    train_config: config/train_natal_weekly.yaml
